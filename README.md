# Cleaning Insurance Direct

ConnectQuote Netlify shell for **cleaning** segment. Logic lives on CID-PDF-API (`/static/connectquote-intake.js`).

- **Live:** `connectquote.html` (root redirects here)
- **Domain:** configured in Netlify → custom domain

Deploy: connect repo to Netlify, publish directory `Netlify/`.
